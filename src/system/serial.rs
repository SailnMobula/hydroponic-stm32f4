fn _usb() {
    // if !usb_dev.poll(&mut [&mut serial]) {
    //     continue;
    // }
    // let word = "Hey, serial";

    // let mut buffer = [0; 64];
    // let mut test: &mut [u16] = &mut buffer;
    // test.write(word.as_bytes()).unwrap();

    // let mut write_offset = 0;
    // while write_offset < buffer.len() {
    //     match serial.write(&buffer[write_offset..buffer.len()]) {
    //         Ok(len) if len > 0 => {
    //             write_offset += len;
    //         }
    //         _ => {}
    //     }
    // }
}
